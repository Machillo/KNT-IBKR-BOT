# IBKR connection
IBKR_HOST=127.0.0.1
# Paper: TWS=7497 / IB Gateway=4002
IBKR_PORT=4002
IBKR_CLIENT_ID=901
IBKR_ACCOUNT=
IBKR_READONLY=false
ALLOW_LIVE_TRADING=false

# Connection/reconnect
IBKR_CONNECTION_TIMEOUT=10
IBKR_RECONNECT_DELAY=5
IBKR_MAX_RECONNECT_ATTEMPTS=10

# Risk hard limits
MAX_TRADE_RISK_PCT=0.10
MAX_DAILY_LOSS_PCT=0.10
MAX_POSITION_PCT=0.10
KILL_SWITCH_ENABLED=true
# Keep true until the armed liquidation flow has been Paper-tested.
KILL_SWITCH_DRY_RUN=true

# 1=Live, 2=Frozen, 3=Delayed, 4=Delayed Frozen
# Paper ENTRIES require live data (1); with 3/4 the executor refuses every entry
# (shadow/research still work). Delayed quotes are minutes old.
MARKET_DATA_TYPE=3

# Autonomous runtime
SUPERVISOR_POLL_SECONDS=15
REQUIRE_FLAT_STARTUP=true
RUN_BROKER_SMOKE_TESTS=false
RUN_DISCOVERY_PROBE=false
# Rows requested from EACH dynamic scanner (IBKR scanner max is 50).
DISCOVERY_ROWS=25
# Maximum discovered instruments that receive market-data snapshots per cycle.
UNIVERSE_QUOTE_BUDGET=40
# Maximum ranked/eligible instruments that receive historical deep analysis per cycle.
SHADOW_MAX_CANDIDATES=12
AUTONOMOUS_TRADING_ENABLED=false
# Autonomous paper orders ALSO need AUTONOMOUS_PAPER_ACK exported in the shell for one
# session. Never put the ACK in this file: a persisted ACK is refused.

# Multi-strategy Paper Alpha shadow mode (no strategy orders)
SHADOW_TRADING_ENABLED=true
SHADOW_INTERVAL_SECONDS=900
# Sticky multi-day drawdown lock from the persisted high-water mark (<= 0.5; human reset only)
MAX_DRAWDOWN_PCT=0.15
